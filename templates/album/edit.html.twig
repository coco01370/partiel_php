{% extends 'base.html.twig' %}

{% block title %}Edit Album{% endblock %}

{% block body %}
<div class="container">
    <h1>Edit Album</h1>

    {{ include('album/_form.html.twig', {'button_label': 'Update'}) }}

    <button class="btn btn-primary"><a href="{{ path('album_index') }}" style="color: white;">back to list</a></button>

    {{ include('album/_delete_form.html.twig') }}
</div>
{% endblock %}
